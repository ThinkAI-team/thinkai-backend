info:
  name: đăng nhập
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8081/auth/login
  body:
    type: json
    data: |
      {
        "email": "admin@example.com",
        "password": "admin@12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
